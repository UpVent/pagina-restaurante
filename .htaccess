# Protect the htaccess file
<Files ".htaccess">
       Order Allow,Deny
       Deny from all
</Files>

# Protect the git folder
<Directory .git>
          Order allow,deny
          Deny from all
</Directory>

# Prevent directory listings
Options All -Indexes
